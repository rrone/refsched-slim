{
  "name": "slim-skeleton",
  "version": "1.0.0",
  "description": "Slim Framework 3 application to provide report support for WordPress site.",
  "main": "gulpfile.js",
  "scripts": {
    "gulp-build": "./node_modules/.bin/gulp build",
    "gulp-watch": "./node_modules/.bin/gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "dist.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0"
  },
  "dependencies": {
    "bootstrap": "^3.3.7",
    "jquery": "^3.1.0",
    "normalize.css": "^4.2.0",
    "purecss": "^0.6.0"
  }
}
